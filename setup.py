from setuptools import setup

setup(
   name='FusedFourierKAN',
   version='0.1',
   description='Implement fused operator for fourierKAN',
   author='GistNoesis',
   author_email='gistnoesis@gmail.com',
   packages=['FusedFourierKAN'],  #same as name
   install_requires=[], #external packages as dependencies
)